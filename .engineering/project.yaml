version: aep.project/1

# Written by `protocol reverse init`. It points; it does not duplicate — a rule restated
# here would be a second copy with no way to say which one is in force. Principles and
# profiles of this project's own go under `principles/` and `profiles/` beside this file.
protocol: adp/1
profile: development.standard
protocols: git+https://github.com/beyond10x/aep.git#595c2cfb4cc4057c7e4ec90933f0d068b3b70cf5

summary: >-
  The authenticated central authority for AEP commands, queries, history, definition bundles and
  projections, persisted transactionally through Entity Runtime providers.
