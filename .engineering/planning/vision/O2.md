---
format: aep.planning-md/1
id: vision:O2
kind: vision
status: archived
title: O2 — Decisions as data, with evidence
summary: Objective O2 of the collection; atlas ROADMAP.md is authoritative and this is its local projection.
owner: atlas
tags:
- objective
revision: 4
---
# O2 — Decisions as data, with evidence

Rules, state and engineering method are declared as data and executed by the component that owns them; a claim carries the evidence behind it.

| | |
|---|---|
| owners | entity-runtime, engineering-protocols, agentic-principles |
| exit evidence | the protocol's artifact model runs on the entity kernel; a principle reaches *supported* on independent evidence |
| source | atlas `ROADMAP.md` § Objectives, row **O2**; the text there is authoritative and this is its planning-store projection |

## Retired — superseded by the atlas authority

This document was a local projection of atlas `ROADMAP.md` § Objectives row **O2**, kept in step by hand. Review 2026-09-15 lane 04 finding **F13** recorded the six `vision:O1`–`O6` mirrors as drafts no epic decomposes, and decision item **18** took the default: name atlas in `.engineering/workspace.yaml` and retire the six. The objective is read from its owner as `atlas/vision:O2`; nothing in this repository restates it.

## Retained as a relation target, not as a source

This document is retained as the target of this store's `serves:` edges and for nothing else. Thirty
artifacts still declare `serves: vision:O2` — five epics, one initiative and twenty-four stories —
and an edge may not point at a file that is not there, so the mirror stays. Do not read it as the
objective.

**The objective is `atlas/vision:O2`**, which projects atlas `ROADMAP.md` § Objectives row **O2**.
That row supersedes every word above it on this page, including the table: what stands above is a
2026-09-15 snapshot, and decision item **18** retired the hand-keeping that used to hold it in step.

The 30 edges were not repointed because no spelling names a target in another workspace member.
`.engineering/workspace.yaml` declares `atlas` and `aep plan workspace show atlas/vision:O2`
resolves it, but `aep plan artifact relate <id> serves atlas/vision:O2` is refused — *"does not hold
`atlas/vision:O2`, so … would be an edge to nothing"* — and `aep plan artifact new --relate
serves:atlas/vision:O2` is refused earlier still, by the id grammar itself: *"invalid locator
identifier `ep://planning/store/atlas/vision/O2`: the kind contains disallowed character `/`"*.
Decision item **7** left that form unadopted; `decision-blocker:cross-store-vision-targets` holds
the evidence and says what would have to exist before these edges can move.
