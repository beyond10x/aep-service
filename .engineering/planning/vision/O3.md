---
format: aep.planning-md/1
id: vision:O3
kind: vision
status: archived
title: O3 — Any harness, observed and compared
summary: Objective O3 of the collection; atlas ROADMAP.md is authoritative and this is its local projection.
owner: atlas
tags:
- objective
revision: 4
---
# O3 — Any harness, observed and compared

A harness is driven from outside, its run is recorded in one vocabulary, and it is scored on the same work as another.

| | |
|---|---|
| owners | metaharness, engineering-protocols |
| exit evidence | the native loop and a vendor loop, same task, same governor, columns that differ only by the harness |
| source | atlas `ROADMAP.md` § Objectives, row **O3**; the text there is authoritative and this is its planning-store projection |

## Retired — superseded by the atlas authority

This document was a local projection of atlas `ROADMAP.md` § Objectives row **O3**, kept in step by hand. Review 2026-09-15 lane 04 finding **F13** recorded the six `vision:O1`–`O6` mirrors as drafts no epic decomposes, and decision item **18** took the default: name atlas in `.engineering/workspace.yaml` and retire the six. The objective is read from its owner as `atlas/vision:O3`; nothing in this repository restates it.

## Retained with nothing pointing at it

This document is retained, and nothing points at it. No artifact in this store declares `serves:
vision:O3` — unlike `vision:O1`, `O2`, `O4` and `O6`, this mirror is not even holding an edge open.
It survives only because `aep plan artifact` retires an item by moving it to `archived` and never by
deleting it; a removed file is what `validate` reports as a deletion no command made.

**The objective is `atlas/vision:O3`**, which projects atlas `ROADMAP.md` § Objectives row **O3**.
That row supersedes every word above it on this page, including the table: what stands above is a
2026-09-15 snapshot, and decision item **18** retired the hand-keeping that used to hold it in step.

`decision-blocker:cross-store-vision-targets` records why the four mirrors that *do* hold edges
cannot be emptied the same way, and what the CLI would need before they can be.
