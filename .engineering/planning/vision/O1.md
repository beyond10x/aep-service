---
format: aep.planning-md/1
id: vision:O1
kind: vision
status: archived
title: O1 — Governed reach
summary: Objective O1 of the collection; atlas ROADMAP.md is authoritative and this is its local projection.
owner: atlas
tags:
- objective
revision: 4
---
# O1 — Governed reach

An agent changes the world only through authority that is declared before the run, checked at the call, and refused by name.

| | |
|---|---|
| owners | harness, substrate, connectors, identity |
| exit evidence | every effectful call in a run's record is attributable to a declared grant; a call outside it is a named refusal, not a missing row |
| source | atlas `ROADMAP.md` § Objectives, row **O1**; the text there is authoritative and this is its planning-store projection |

## Retired — superseded by the atlas authority

This document was a local projection of atlas `ROADMAP.md` § Objectives row **O1**, kept in step by hand. Review 2026-09-15 lane 04 finding **F13** recorded the six `vision:O1`–`O6` mirrors as drafts no epic decomposes, and decision item **18** took the default: name atlas in `.engineering/workspace.yaml` and retire the six. The objective is read from its owner as `atlas/vision:O1`; nothing in this repository restates it.

## Retained as a relation target, not as a source

This document is retained as the target of this store's `serves:` edges and for nothing else.
Fourteen artifacts still declare `serves: vision:O1` — four epics, one initiative and nine stories —
and an edge may not point at a file that is not there, so the mirror stays. Do not read it as the
objective.

**The objective is `atlas/vision:O1`**, which projects atlas `ROADMAP.md` § Objectives row **O1**.
That row supersedes every word above it on this page, including the table: what stands above is a
2026-09-15 snapshot, and decision item **18** retired the hand-keeping that used to hold it in step.

The 14 edges were not repointed because no spelling names a target in another workspace member.
`.engineering/workspace.yaml` declares `atlas` and `aep plan workspace show atlas/vision:O1`
resolves it, but `aep plan artifact relate <id> serves atlas/vision:O1` is refused — *"does not hold
`atlas/vision:O1`, so … would be an edge to nothing"* — and `aep plan artifact new --relate
serves:atlas/vision:O1` is refused earlier still, by the id grammar itself: *"invalid locator
identifier `ep://planning/store/atlas/vision/O1`: the kind contains disallowed character `/`"*.
Decision item **7** left that form unadopted; `decision-blocker:cross-store-vision-targets` holds
the evidence and says what would have to exist before these edges can move.
